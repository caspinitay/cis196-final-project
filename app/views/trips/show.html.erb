<p id="notice"><%= notice %></p>

<h1><%= @trip.title %></h1>

<p><%= @trip.description %></p>
<P> Cost: $<%= @trip.cost %></P>

<%= link_to 'Edit', edit_trip_path(@trip) %> |
<%= link_to 'Back', trips_path %>
